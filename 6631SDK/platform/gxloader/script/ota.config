# OTA bin flash addr
OTA_FORCE_FLASH_ADDR = 0x020000
# OTA bin size, must bigger than real size of OTA bin
OTA_FORCE_FLASH_SIZE = 0x050000
# OTA showlogo used buffer size
SPP_BUF_SIZE = 0x130000
# OTA GUI used buffer size
OSD_BUF_SIZE = 0x130000
