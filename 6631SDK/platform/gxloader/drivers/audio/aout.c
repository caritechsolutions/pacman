#include "version_autogenerated.h"

#if defined(CONFIG_ARCH_ARM_SIRIUS) || defined(CONFIG_ARCH_CKMMU_TAURUS) || defined(CONFIG_ARCH_CKMMU_GEMINI) || defined(CONFIG_ARCH_CKMMU_CYGNUS)
#include "aout.h"
#include "aout_reg.h"
#include "cpu_config.h"

static AoutRegs optReg = {
#if defined(CONFIG_ARCH_ARM_SIRIUS)
	.i2sRegs_0 = (AoutI2SRegs *)0x8ad00000, //I2S_BASE_ADDR_0
	.i2sRegs_1 = (AoutI2SRegs *)0x8ad00040, //I2S_BASE_ADDR_1
#elif defined(CONFIG_ARCH_CKMMU_TAURUS) || defined(CONFIG_ARCH_CKMMU_GEMINI) || defined(CONFIG_ARCH_CKMMU_CYGNUS)
	.i2sRegs_0 = (AoutI2SRegs *)0xa4a00000, //I2S_BASE_ADDR_0
	.i2sRegs_1 = (AoutI2SRegs *)0xa4a00040, //I2S_BASE_ADDR_1
#endif

};

//#define LODAC_INIT

#ifdef LODAC_INIT
#include "../jpeg/gx3201_jpeg.h"
#include "../gdi/gx3201_gdi.h"
static volatile struct config_regs *taurus_clock_reg = (void *)CONFIG_BASE_MMU;

static int aout_dac_slow_enable(unsigned int power, unsigned int enable)
{
	if (power) {
		if (enable) {
			REG_SET_FIELD(&(taurus_clock_reg->cfg_audio_lcodec_config1), (0x3<<24), 2, 24);
			REG_SET_BIT  (&(taurus_clock_reg->cfg_audio_lcodec_config1), 19);
			REG_CLR_BIT  (&(taurus_clock_reg->cfg_audio_lcodec_config1), 27);
		} else {
			REG_SET_BIT  (&(taurus_clock_reg->cfg_audio_lcodec_config1), 31);
			REG_SET_FIELD(&(taurus_clock_reg->cfg_audio_lcodec_config1), (0x3<<24), 0, 24);
			REG_SET_BIT  (&(taurus_clock_reg->cfg_audio_lcodec_config1), 27);
		}
	} else {
		if (enable) {
			REG_SET_BIT  (&(taurus_clock_reg->cfg_audio_lcodec_config1), 29);
			REG_SET_BIT  (&(taurus_clock_reg->cfg_audio_lcodec_config1), 28);
			REG_SET_FIELD(&(taurus_clock_reg->cfg_audio_lcodec_config1), (0x3<<24), 3, 24);
			REG_CLR_BIT  (&(taurus_clock_reg->cfg_audio_lcodec_config1), 26);
			REG_SET_BIT  (&(taurus_clock_reg->cfg_audio_lcodec_config1), 31);
		} else {
			REG_CLR_BIT  (&(taurus_clock_reg->cfg_audio_lcodec_config1), 31);
			REG_SET_FIELD(&(taurus_clock_reg->cfg_audio_lcodec_config1), (0x3<<24), 0, 24);
			REG_SET_BIT  (&(taurus_clock_reg->cfg_audio_lcodec_config1), 26);
		}
	}

	return 0;
}

static int aout_dac_slow_config(unsigned int step_num, unsigned int skip_num)
{
	REG_SET_FIELD(&(taurus_clock_reg->cfg_audio_lcodec_config1), (0xf<<20), step_num, 20);
	REG_SET_VAL  (&(taurus_clock_reg->cfg_audio_lcodec_config2), skip_num);
	return 0;
}

static int aout_dac_enable(unsigned int mclock)
{
	int value = 0;

	REG_SET_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 0);

	REG_SET_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);
	REG_CLR_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);

	REG_SET_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);
	REG_CLR_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);
	switch (mclock) {
		case 256:
			REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl),
					((0<<12)|(0xa8<<4)|(1<<2)|(1<<0)));
			break;
		case 384:
			REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl),
					((0<<12)|(0xa0<<4)|(1<<2)|(1<<0)));
			break;
		case 512:
			REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl),
					((0<<12)|(0x98<<4)|(1<<2)|(1<<0)));
			break;
		case 768:
			REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl),
					((0<<12)|(0x90<<4)|(1<<2)|(1<<0)));
			break;
		case 1024:
			REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl),
					((0<<12)|(0x88<<4)|(1<<2)|(1<<0)));
			break;
		case 1536:
			REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl),
					((0<<12)|(0x80<<4)|(1<<2)|(1<<0)));
			break;
		case 128:
			REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl),
					((0<<12)|(0xb8<<4)|(1<<2)|(1<<0)));
			break;
		case 192:
			REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl),
					((0<<12)|(0xb0<<4)|(1<<2)|(1<<0)));
			break;
		default:
			REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl),
					((0<<12)|(0xa8<<4)|(1<<2)|(1<<0)));
			break;
	}
	REG_SET_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);
	REG_CLR_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);

	REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl), ((0<<12)|(1<<3)|(1<<0)));
	REG_SET_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);
	REG_CLR_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);

	value = REG_GET_VAL(&(taurus_clock_reg->cfg_audio_codec_data)) & 0xfffffffe;
	REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl), ((0<<12)|((0x00|value)<<4)|(1<<2)|(1<<0)));
	REG_SET_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);
	REG_CLR_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);

	REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl), ((3<<12)|(0x7f<<4)|(1<<2)|(1<<0)));
	REG_SET_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);
	REG_CLR_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);

	REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl), ((0<<12)|((0x01|value)<<4)|(1<<2)|(1<<0)));
	REG_SET_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);
	REG_CLR_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);

	REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl), ((3<<12)|(0x7f<<4)|(1<<2)|(1<<0)));
	REG_SET_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);
	REG_CLR_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);

	REG_SET_VAL(&(taurus_clock_reg->cfg_audio_codec_ctrl), ((2<<12)|(0x38<<4)|(1<<2)|(1<<0)));
	REG_SET_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);
	REG_CLR_BIT(&(taurus_clock_reg->cfg_audio_codec_ctrl), 1);

	return 0;
}
#endif

int aout_i2s_play_zero(unsigned int samplefre, unsigned int channels)
{
	aout_src_enable_reset     (&optReg, SRC_R0|SRC_R1|SRC_R2|SRC_R3, 0);
	aout_src_enable_silent_end(&optReg, SRC_R0|SRC_R1|SRC_R2|SRC_R3, 0);

#ifdef LODAC_INIT
	aout_dac_slow_enable(1, 0);
	aout_dac_slow_config(0x1, 0x40);
	aout_dac_enable(1024);
	aout_dac_slow_enable(1, 1);
	mdelay(50);
	aout_dac_slow_enable(0, 0);
	aout_dac_slow_config(1, 0xf);
	aout_dac_slow_enable(0, 0);
#endif

	aout_dac_set_mclk  (&optReg, DAC_MCLK_1024FS);
	aout_dac_set_bclk  (&optReg, DAC_BCLK_64FS);
	aout_dac_set_format(&optReg, DAC_FORMAT_I2S);
	aout_src_set_pcm_output_bits (&optReg, (SRC_R0|SRC_R1), PCM_OUTPUT_24_BITS);

	//解决某些电视机hdmi杂音问题，需要I2S一直有输出状态
	aout_src_set_pcm_channelnum(&optReg, SRC_R0|SRC_R1, channels);
	aout_src_set_pcm_source    (&optReg, SRC_R0|SRC_R1, PCM_SOURCE_CPU);
	aout_src_set_pcm_value     (&optReg, SRC_R0|SRC_R1, 0x0);
	aout_src_enable_playback   (&optReg, SRC_R0|SRC_R1, 0x1);
	aout_src_enable_work       (&optReg, SRC_R0|SRC_R1, 0x1);
	return 0;
}
#else
int aout_i2s_play_zero(unsigned int samplefre, unsigned int channels)
{
	return 0;
}
#endif
