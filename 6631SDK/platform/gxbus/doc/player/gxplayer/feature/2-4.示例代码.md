#### DVB录制-单节目录制-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "Recorder";
	char *srcurl = "dvbs://fre:0&polar:1&symbol:27500&22k:1&vpid:120&apid:131&pcrpid:120&vcodec:0&acodec:1&tuner:0&scramble:0&pmt:100&tsid:0&dmxid:0&pls_num:0";
	char *dsturl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";

	/*
	 * 参数配置,需要播放器初始化之前
	 */
	GxBus_ConfigSetInt(PLAYER_CONFIG_RECORD_CACHED_MEMSIZE, 3*1024*1024);
	GxBus_ConfigSetInt(PLAYER_CONFIG_RECORD_TSW_MEMSIZE, 188*1024);

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR
	 */
	GxPlayer_ModuleRegisterStreamDVB();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterRecorderDVB();
	GxPlayer_ModuleRegisterStreamFILE();

	/*
	 * 启动录制
	 * 分卷录制: dsturl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr
	 * 分段录制: dsturl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.pvr
	 */
	GxPlayer_MediaRecord(name, srcurl, dsturl);

	...

	/*
	 * 停止录制
	 */
	GxPlayer_MediaStop(name);
}
```

#### DVB录制-单频点录制-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "Recorder";
	char *srcurl = "dvbs://fre:0&polar:1&symbol:27500&22k:1&vpid:120&apid:131&pcrpid:120&vcodec:0&acodec:1&tuner:0&scramble:0&pmt:100&tsid:0&dmxid:0&pls_num:0";
	char *dsturl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";
	PlayerRecordConfig config;

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR
	 */
	GxPlayer_ModuleRegisterStreamDVB();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterRecorderDVB();
	GxPlayer_ModuleRegisterStreamFILE();

	/*
	 * 配置单频点录制
	 */
	memset(&config, 0, sizeof(PlayerRecordConfig));
	config.ext_info.is_rawts = 1;
	GxPlayer_MediaRecordConfig(name, &config);

	/*
	 * 启动录制
	 * 分卷录制: dsturl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr
	 * 分段录制: dsturl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.pvr
	 */
	GxPlayer_MediaRecord(name, srcurl, dsturl);

	...

	/*
	 * 停止录制
	 */
	GxPlayer_MediaStop(name);
}
```

#### DVB录制-加扰录制-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "Recorder";
	char *srcurl = "dvbs://fre:0&polar:1&symbol:27500&22k:1&vpid:120&apid:131&pcrpid:120&vcodec:0&acodec:1&tuner:0&scramble:0&pmt:100&tsid:0&dmxid:0&pls_num:0";
	char *dsturl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";
	PlayerRecordConfig config;

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR
	 */
	GxPlayer_ModuleRegisterStreamDVB();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterRecorderDVB();
	GxPlayer_ModuleRegisterStreamFILE();

	/*
	 * 配置单频点录制
	 */
	memset(&config, 0, sizeof(PlayerRecordConfig));
	config.ext_info.is_encrypt = 1;
	GxPlayer_MediaRecordConfig(name, &config);

	/*
	 * 启动录制
	 * 分卷录制: dsturl = xxxx.dvr
	 * 分段录制: dsturl = xxxx.pvr
	 */
	GxPlayer_MediaRecord(name, srcurl, dsturl);

	...

	/*
	 * 停止录制
	 */
	GxPlayer_MediaStop(name);
}
```

#### DVB录制-扩展录制-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "Recorder";
	char *srcurl = "dvbs://fre:0&polar:1&symbol:27500&22k:1&vpid:120&apid:131&pcrpid:120&vcodec:0&acodec:1&tuner:0&scramble:0&pmt:100&tsid:0&dmxid:0&pls_num:0";
	char *dsturl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR
	 */
	GxPlayer_ModuleRegisterStreamDVB();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterRecorderDVB();
	GxPlayer_ModuleRegisterStreamFILE();

	/*
	 * 配置扩展录制
	 */
	PlayerRecordConfig config;
	int ext_num = 0;
	char user_data[188] = {0x47,...};
	memset(&config, 0, sizeof(PlayerRecordConfig));
	config.ext_info.ext_types[ext_num]             = DEMUX_SLOT_AUDIO;
	config.ext_info.ext_pids_content[ext_num]      = PLAYER_MEDIA_AUDIO;
	config.ext_info.ext_pids_content_type[ext_num] = AUDIO_CODEC_MPEG1;
	config.ext_info.ext_pids[ext_num]              = 0x100;
	ext_num++;
	config.ext_info.ext_types[ext_num]             = DEMUX_SLOT_PES;
	config.ext_info.ext_pids_content[ext_num]      = PLAYER_MEDIA_SUB;
	config.ext_info.ext_pids_content_type[ext_num] = SUBTITLE_CODEC_DVB;
	config.ext_info.ext_pids[ext_num]              = 0x102;
	ext_num++;
	config.ext_info.ext_types[ext_num]             = DEMUX_SLOT_PES_AUDIO;
	config.ext_info.ext_pids_content[ext_num]      = PLAYER_MEDIA_AUDIO_DESC;
	config.ext_info.ext_pids_content_type[ext_num] = AUDIO_CODEC_MPEG1;
	config.ext_info.ext_pids[ext_num]              = 0x101;
	ext_num++;
	config.ext_info.ext_num = ext_num;
	config.ext_data.have_pmt = 0;
	config.ext_data.userdata_len = 188;
	memcpy(config.ext_data.userdata, user_data, 188);
	GxPlayer_MediaRecordConfig(name, &config);

	/*
	 * 启动录制
	 * 分卷录制: dsturl = xxxx.dvr
	 * 分段录制: dsturl = xxxx.pvr
	 */
	GxPlayer_MediaRecord(name, srcurl, dsturl);

	...

	/*
	 * 停止录制
	 */
	GxPlayer_MediaStop(name);
}
```

#### DVB录制-安全录制与非安全录制-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "Recorder";
	char *srcurl = "dvbs://fre:0&polar:1&symbol:27500&22k:1&vpid:120&apid:131&pcrpid:120&vcodec:0&acodec:1&tuner:0&scramble:0&pmt:100&tsid:0&dmxid:0&pls_num:0";
	char *dsturl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";
	int dmxid = 2;

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR
	 */
	GxPlayer_ModuleRegisterStreamDVB();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterRecorderDVB();
	GxPlayer_ModuleRegisterStreamFILE();

	/*
	 * 修改dmxid参数
	 * 以sirius-NNE芯片为例子,方案没有对demuxer dvr限制时.
	 * dmxid:0|1 属于安全录制
	 * dmxid:2|3 属于非安全录制
	 */
	GxUrl_SetItem(srcurl, GX_URL_KEY_DMXID, dmxid, PLAYER_URL_LONG);

	/*
	 * 启动录制
	 * 分卷录制: dsturl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr
	 * 分段录制: dsturl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.pvr
	 */
	GxPlayer_MediaRecord(name, srcurl, dsturl);

	...

	/*
	 * 停止录制
	 */
	GxPlayer_MediaStop(name);
}
```


#### DVB录制-用户加密和非用户加密-示例代码
```c
#include "gxplayer_pvr.h"
#include "gxsecure/gxtfm_api.h"
static char *PVR_ENC_KEY[5] = {
       "12345678",
       "23456781",
       "34567812",
       "45678123",
       "56781234"
};

static int _dvr_tfm_copy(unsigned char* src, unsigned char* dst, unsigned int size, int encrpyt)
{
	int ret = -1;
	GxTfmCrypto param = {0};

	memset(&param, 0, sizeof(GxTfmCrypto));
	param.module        = TFM_MOD_M2M;
	param.alg           = TFM_ALG_AES128;
	param.opt           = TFM_OPT_ECB;
	param.src.id        = TFM_SRC_MEM;
	param.dst.id        = TFM_DST_MEM;
	param.input.buf     = src;
	param.input.length  = size;
	param.output.buf    = dst;
	param.output.length = size;
	param.even_key.id   = TFM_KEY_SSUK;
	param.odd_key.id    = TFM_KEY_SSUK;
	param.flags         = TFM_FLAG_CRYPT_EVEN_KEY_VALID | TFM_FLAG_CRYPT_ODD_KEY_VALID |
		TFM_FLAG_CRYPT_TS_PACKET_MODE | TFM_FLAG_CRYPT_SWITCH_CLR | TFM_FLAG_CRYPT_HW_PVR_MODE;

	if (encrpyt) {
		param.input_paddr = (unsigned int)src;
		ret = GxTfm_Encrypt(&param);
	} else {
		param.output_paddr = (unsigned int)dst;
		ret = GxTfm_Decrypt(&param);
	}
	if (ret < 0) {
		gxloge("DVR %s Copy error, ret = %d\n", encrpyt ? "encrpyt" : "decrpyt", ret);
		return -1;
	}

	return 0;
}

static int pvr_encrypt(PlayerPvrEncryptPara *para)
{
	if ((para->ibuf_type == PLAYER_PVR_BUF_NORMAL) &&
			(para->obuf_type == PLAYER_PVR_BUF_NORMAL)) {
		if ((para->dvr.i_size % 8) == 0) {
			unsigned int i = 0;
			extern cyg_uint32 hal_ioremap_uncached(cyg_uint32);
			unsigned char *src = para->dvr.i_buf;
			unsigned char *dst = para->dvr.o_buf;
			unsigned char *key = (unsigned char*)PVR_ENC_KEY[0];

			src = (para->ibuf_type == PLAYER_PVR_BUF_NORMAL) ? src : (char*)hal_ioremap_uncached((cyg_uint32)src);
			dst = (para->ibuf_type == PLAYER_PVR_BUF_NORMAL) ? dst : (char*)hal_ioremap_uncached((cyg_uint32)dst);
			for (i = 0; i < para->dvr.i_size; i++)
				dst[i] = src[i] ^ key[i % 8];
			para->dvr.o_size   = para->dvr.i_size;
		} else {
			printf("%s %d: error\n", __func__, __LINE__);
			return -1;
		}
	} else if ((para->ibuf_type == PLAYER_PVR_BUF_SECURE) &&
			(para->obuf_type == PLAYER_PVR_BUF_NORMAL)) {
		_dvr_tfm_copy(para->dvr.i_buf, para->dvr.o_buf, para->dvr.i_size, 1);
	} else {
		printf("%s %d: error\n", __func__, __LINE__);
		return -1;
	}

	return 0;
}

static int pvr_decrypt(PlayerPvrEncryptPara *para)
{
	if ((para->ibuf_type == PLAYER_PVR_BUF_NORMAL) &&
			(para->obuf_type == PLAYER_PVR_BUF_NORMAL)) {
		if ((para->dvr.i_size % 8) == 0) {
			unsigned int i = 0;
			extern cyg_uint32 hal_ioremap_uncached(cyg_uint32);
			unsigned char *src = para->dvr.i_buf;
			unsigned char *dst = para->dvr.o_buf;
			unsigned char *key = (unsigned char*)PVR_ENC_KEY[0];

			src = (para->ibuf_type == PLAYER_PVR_BUF_NORMAL) ? src : (char*)hal_ioremap_uncached((cyg_uint32)src);
			dst = (para->ibuf_type == PLAYER_PVR_BUF_NORMAL) ? dst : (char*)hal_ioremap_uncached((cyg_uint32)dst);
			for (i = 0; i < para->dvr.i_size; i++)
				dst[i] = src[i] ^ key[i % 8];
			para->dvr.o_size   = para->dvr.i_size;
		} else {
			printf("%s %d: error\n", __func__, __LINE__);
			return -1;
		}
	} else if ((para->ibuf_type == PLAYER_PVR_BUF_NORMAL) &&
			(para->obuf_type == PLAYER_PVR_BUF_SECURE)) {
		_dvr_tfm_copy(para->dvr.i_buf, para->dvr.o_buf, para->dvr.i_size, 0);
	} else {
		printf("%s %d: error\n", __func__, __LINE__);
		return -1;
	}

	return 0;
}

static PlayerPvrEncryptOps dvr_ops = {
       .name     = "dvr encrypt/decrypt",
       .author   = "linxsh",
       .enc_mode = PLAYER_PVR_DVR_ENCRYPT,
       .encrpyt  = pvr_encrypt,
       .decrpyt  = pvr_decrypt,
};

static PlayerPvrEncryptOps pvr_ops = {
       .name     = "pvr encrypt/decrypt",
       .author   = "linxsh",
       .enc_mode = PLAYER_PVR_PVR_ENCRYPT,
       .encrpyt  = pvr_encrypt,
       .decrpyt  = pvr_decrypt,
};

#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "Recorder";
	char *srcurl = "dvbs://fre:0&polar:1&symbol:27500&22k:1&vpid:120&apid:131&pcrpid:120&vcodec:0&acodec:1&tuner:0&scramble:0&pmt:100&tsid:0&dmxid:0&pls_num:0";
	char *dsturl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";
	PlayerRecordEncrypt encrypt;

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册以及用户加密函数注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR
	 */
	GxPlayer_ModuleRegisterStreamDVB();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterRecorderDVB();
	GxPlayer_ModuleRegisterStreamFILE();
	GxPVRPlayer_RegisterEncrypt(&dvr_ops);
	GxPVRPlayer_RegisterEncrypt(&pvr_ops);

	/*
	 * 用户加密设置
	 * 加密块大小由用户决定,最好188字节对齐
	 */
	memset(&encrypt, 0, sizeof(PlayerRecordEncrypt));
	encrypt.user_encrypt_enable    = 1;
	encrypt.user_encrypt_blocksize = 64*188;
	GxPlayer_MediaRecordEncrypt(name, &encrypt);

	/*
	 * 启动录制
	 * 分卷录制: dsturl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr
	 * 分段录制: dsturl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.pvr
	 */
	GxPlayer_MediaRecord(name, srcurl, dsturl);

	...

	/*
	 * 停止录制
	 */
	GxPlayer_MediaStop(name);
}
```
#### DVB录制-用户循环录制-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "Recorder";
	char *srcurl = "dvbs://fre:0&polar:1&symbol:27500&22k:1&vpid:120&apid:131&pcrpid:120&vcodec:0&acodec:1&tuner:0&scramble:0&pmt:100&tsid:0&dmxid:0&pls_num:0";
	char *dsturl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";
	int volume_size = 100*1024;  //100MB
	int volume_max  = 100;       //100卷
	int ful_stop    = 0;        //开启循环录制
	int resv_size   = 1024*1024;//预留1G
	int max_timems  = 24*60*60*1000;//24h
	PlayerPVRConfig config;

	/*
	 * 参数配置,需要播放器初始化之前
	 */
	GxBus_ConfigSetInt(PLAYER_CONFIG_PVR_RECORD_VOLUME_SIZE,     volume_size);
	GxBus_ConfigSetInt(PLAYER_CONFIG_PVR_RECORD_VOLUME_MAX,      volume_max);
	GxBus_ConfigSetInt(PLAYER_CONFIG_PVR_RECORD_VOLUME_FULLSTOP, ful_stop);
	GxBus_ConfigSetInt(PLAYER_CONFIG_PVR_RECORD_RESERVE_SIZE,    resv_size);

	/*
	 * 播放子系统初始之前
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR
	 */
	GxPlayer_ModuleRegisterStreamDVB();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterRecorderDVB();
	GxPlayer_ModuleRegisterStreamFILE();

	/*
	 * 接口配置,重启后无效
	 */
	memset(&config, 0, sizeof(PlayerPVRConfig));
	config.volume_sizemb   = volume_size;
	config.volume_maxnum   = volume_max;
	config.volume_fullstop = ful_stop;
	config.reserve_sizemb  = resv_size;
	config.volume_maxtimes = max_timems;
	GxPlayer_SetPVRConfig(&config);

	/*
	 * 启动录制
	 */
	GxPlayer_MediaRecord(name, srcurl, dsturl);

	...

	/*
	 * 停止录制
	 */
	GxPlayer_MediaStop(name);
}
```

#### DVB录制-录制节目编辑-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "Recorder";
	char *srcurl = "dvbs://fre:0&polar:1&symbol:27500&22k:1&vpid:120&apid:131&pcrpid:120&vcodec:0&acodec:1&tuner:0&scramble:0&pmt:100&tsid:0&dmxid:0&pls_num:0";
	char *dsturl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.pvr";

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 */
	GxPlayer_ModuleRegisterStreamDVB();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterRecorderDVB();
	GxPlayer_ModuleRegisterStreamFILE();
	GxPlayer_ModuleRegisterStreamPVR();

	/*
	 * 启动录制
	 */
	GxPlayer_MediaRecord(name, srcurl, dsturl);

	...

	/*
	 * 录制过程中编辑
	 */
	PlayerPvrEventInfo evtinfo;
	PlayerPvrSegmentInfo seginfo;
	PlayerPvrSegmentList seglist;
	char *seg_prefix = NULL;
	char *evt_prefix = NULL;

	memset(&evtinfo, 0, sizeof(PlayerPvrEventInfo));
	evtinfo.desc         = "<家有儿女>";
	evtinfo.chapter      = "第一集";
	evtinfo.chapter_desc = "主演:";
	evtinfo.start_time   = "09:00";
	evtinfo.end_time     = "10:00";
	evt_prefix = "01";
	GxPVRPlayer_OpenEvent(name, evt_prefix, &evtinfo);
	seg_prefix = "01";
	memset(&seginfo, 0, sizeof(PlayerPvrSegmentInfo));
	seginfo.desc = "片头";
	GxPVRPlayer_NewSegment(name, seg_prefix, &seginfo);
	memser(&seglist, 0, sizeof(PlayerPvrSegmentList));
	seglist.count  = 1;
	seglist.prefix = &seg_prefix;
	GxPVRPlayer_AddSegmentToEvent(name, evt_prefix, seglist);

	...

	seg_prefix = "02";
	memset(&seginfo, 0, sizeof(PlayerPvrSegmentInfo));
	seginfo.desc = "正片";
	GxPVRPlayer_NewSegment(name, seg_prefix, &seginfo);
	memser(&seglist, 0, sizeof(PlayerPvrSegmentList));
	seglist.count  = 1;
	seglist.prefix = &seg_prefix;
	GxPVRPlayer_AddSegmentToEvent(name, evt_prefix, seglist);

	...

	seg_prefix = "03";
	memset(&seginfo, 0, sizeof(PlayerPvrSegmentInfo));
	seginfo.desc = "片尾";
	GxPVRPlayer_NewSegment(name, seg_prefix, &seginfo);
	memser(&seglist, 0, sizeof(PlayerPvrSegmentList));
	seglist.count  = 1;
	seglist.prefix = &seg_prefix;
	GxPVRPlayer_AddSegmentToEvent(name, evt_prefix, seglist);
	/*
	 * 停止录制
	 */
	GxPlayer_MediaStop(name);

	...

	/*
	 * 录制后编辑
	 */
	PvrPvrInfo info;
	char evt_file[1024];
	handle_t handle = -1;
	PvrEventListEdit edit;
	PvrFileAttr seg_array[3];

	GxPVR_GetPVRInfo(dsturl, &info);
	sprintf(evt_file,"%s/%s/%s",info->path,"event","02.evt");
	handle = GxPVREventList_Open(evt_file);
	memset(seg_array, 0, 3 * sizeof(PvrFileAttr));
	seg_array[0].prefix = "01";
	seg_array[1].prefix = "02";
	seg_array[2].prefix = "03";
	memset(&edit, 0, sizeof(PvrEventListEdit));
	edit.event.desc         = "<家有儿女>";
	edit.event.chapter      = "第一集";
	edit.event.chapter_desc = "主演:";
	edit.event.start_time   = "09:00";
	edit.event.end_time     = "10:00";
	edit.event.segment_count= 3;
	edit.event.segment_array= seg_array;
	GxPVREventList_Add(handle, &edit);
	GxPVREventList_Close(handle);
	GxPVR_FreePVRInfo(&info);
}
```

#### DVB录制-MEM储存-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "Recorder";
	char *srcurl = "dvbs://fre:0&polar:1&symbol:27500&22k:1&vpid:120&apid:131&pcrpid:120&vcodec:0&acodec:1&tuner:0&scramble:0&pmt:100&tsid:0&dmxid:0&pls_num:0";
	char dsturl[64];
	char *rec_mem  = NULL;
	int   rec_size = 5*1024*1024;

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 */
	GxPlayer_ModuleRegisterStreamDVB();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterRecorderDVB();
	GxPlayer_ModuleRegisterStreamMEM();
	
	/*
	 * 配置扩展录制
	 */
	PlayerRecordConfig config;
	int ext_num = 0;
	char user_data[188] = {0x47,...};
	memset(&config, 0, sizeof(PlayerRecordConfig));
	config.ext_info.ext_types[ext_num]             = DEMUX_SLOT_AUDIO;
	config.ext_info.ext_pids_content[ext_num]      = PLAYER_MEDIA_AUDIO;
	config.ext_info.ext_pids_content_type[ext_num] = AUDIO_CODEC_MPEG1;
	config.ext_info.ext_pids[ext_num]              = 0x100;
	ext_num++;
	config.ext_info.ext_types[ext_num]             = DEMUX_SLOT_PES;
	config.ext_info.ext_pids_content[ext_num]      = PLAYER_MEDIA_SUB;
	config.ext_info.ext_pids_content_type[ext_num] = SUBTITLE_CODEC_DVB;
	config.ext_info.ext_pids[ext_num]              = 0x102;
	ext_num++;
	config.ext_info.ext_types[ext_num]             = DEMUX_SLOT_PES_AUDIO;
	config.ext_info.ext_pids_content[ext_num]      = PLAYER_MEDIA_AUDIO_DESC;
	config.ext_info.ext_pids_content_type[ext_num] = AUDIO_CODEC_MPEG1;
	config.ext_info.ext_pids[ext_num]              = 0x101;
	ext_num++;
	config.ext_info.ext_num = ext_num;
	config.ext_data.have_pmt = 0;
	config.ext_data.userdata_len = 188;
	memcpy(config.ext_data.userdata, user_data, 188);
	GxPlayer_MediaRecordConfig(name, &config);
	/*
	 * 启动录制
	 */
	rec_mem = GxCore_Malloc(rec_size);
	snprintf(dsturl, 64, "mem://fmt:4&hwsafe:0&addr:%p&size:%d", rec_mem, rec_size);
	GxPlayer_MediaRecord(name, srcurl, dsturl);

	...

	/*
	 * 停止录制
	 */
	GxPlayer_MediaStop(name);
	GxCore_Free(rec_mem);
}
```

#### FM录制-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "Recorder";
	char *srcurl = "fm://fre:95000&tuner:2";
	char *dsturl = "/mnt/usb01/95000.fm.wav";

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 */
	GxPlayer_ModuleRegisterStreamFM();
	GxPlayer_ModuleRegisterDemuxerFM();
	GxPlayer_ModuleRegisterRecorderFM();
	GxPlayer_ModuleRegisterStreamFILE();

	/*
	 * 启动录制
	 */
	GxPlayer_MediaRecord(name, srcurl, dsturl);

	...

	/*
	 * 停止录制
	 */
	GxPlayer_MediaStop(name);
}
```

#### 回放录制文件-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "playBacker";
	char *srcurl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR();
	 */
	GxPlayer_ModuleRegisterStreamFILE();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterDemuxerSWTS();

	/*
	 * 播放前,获取文件节目信息
	 */
	PlayerProgInfo info;
	memset(&info, 0, sizeof(PlayerProgInfo));
	GxPlayer_MediaGetProgInfoByUrl(srcurl, &info);
	printf("filesize: %lld, duration: %lld\n", info.filesize, info.duration);
	/*
	 * 播放前,获取文件录制信息
	 */
	PlayerRecordConfig config;
	memset(&config, 0, sizeof(PlayerRecordConfig));
	GxPlayer_MediaGetRecordConfigByUrl(srcurl, &config);
	printf("extra pid num: %d, user table len: %d\n", config.ext_info.ext_num, config.ext_data.userdata_len);

	/*
	 * 启动播放
	 * 分卷录制: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr
	 * 分段录制: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.pvr
	 */
	GxPlayer_MediaPlay(name, srcurl, 0, 50, NULL);
	...

	/*
	 * 播放中,获取文件节目信息
	 */
	memset(&info, 0, sizeof(PlayerProgInfo));
	GxPlayer_MediaGetProgInfoByName(name, &info);
	printf("filesize: %lld, duration: %lld\n", info.filesize, info.duration);
	/*
	 * 播放中,获取文件录制信息
	 */
	memset(&config, 0, sizeof(PlayerRecordConfig));
	GxPlayer_MediaGetRecordConfigByName(name, &config);
	printf("extra pid num: %d, user table len: %d\n", config.ext_info.ext_num, config.ext_data.userdata_len);

	/*
	 * 停止播放
	 */
	GxPlayer_MediaStop(name);
}
```

#### 回放录制文件-控制回放dmxid-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "playBacker";
	char *srcurl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";
	int dmxid = 0;
	PlayerPlayPVRConfig config;

	/*
	 * 参数配置,需要播放器初始化之前
	 */
	GxBus_ConfigSetInt(PLAYER_CONFIG_PVR_PLAY_DEMUX_ID, dmxid);

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR();
	 */
	GxPlayer_ModuleRegisterStreamFILE();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterDemuxerSWTS();

	/*
	 * 接口配置,重启后无效
	 */
	memset(&config, 0, sizeof(PlayerPlayPVRConfig));
	config.demux_id = dmxid;
	GxPlayer_SetPlayPVRConfig(&config);

	/*
	 * 启动播放
	 * 分卷录制: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr
	 * 分段录制: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.pvr
	 */
	GxPlayer_MediaPlay(name, srcurl, 0, 50, NULL);

	...

	/*
	 * 停止播放
	 */
	GxPlayer_MediaStop(name);
}
```

#### 回放录制文件-控制回放同步方式-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "playBacker";
	char *srcurl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";
	int syncmode = FIXD_APTS_RECOVER;

	/*
	 * 参数配置,需要播放器初始化之前
	 */
	GxBus_ConfigSetInt(PLAYER_CONFIG_PVR_PLAYBACK_HWTS_SYNCMODE, syncmode);

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR();
	 */
	GxPlayer_ModuleRegisterStreamFILE();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterDemuxerSWTS();

	/*
	 * 启动播放
	 * 分卷录制: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr
	 * 分段录制: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.pvr
	 */
	GxPlayer_MediaPlay(name, srcurl, 0, 50, NULL);

	...

	/*
	 * 停止播放
	 */
	GxPlayer_MediaStop(name);
}
```

#### 回放录制文件-播放控制-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "playBacker";
	char *srcurl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";
	int syncmode = FIXD_APTS_RECOVER;

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR();
	 */
	GxPlayer_ModuleRegisterStreamFILE();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterDemuxerSWTS();

	/*
	 * 启动播放
	 * 分卷录制: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr
	 * 分段录制: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439.ts.pvr
	 */
	GxPlayer_MediaPlay(name, srcurl, 0, 50, NULL);
	...

	/*
	 * 暂停恢复
	 */
	GxPlayer_MediaPause(name);
	...
	GxPlayer_MediaResume(name);
	...
	/*
	 * 快进快退
	 */
	GxPlayer_MediaSeek(name, 60*1000, SEEK_ORIGIN_SET);
	...
	/*
	 * 倍进倍退
	 */
	GxPlayer_MediaSpeed(name, -2.0);
	...
	GxPlayer_MediaSpeed(name, 4.0);
	...
	GxPlayer_MediaSpeed(name, 1.0);
	...
	/*
	 * 切换音频
	 */
	GxPlayer_MediaAudioSwitch(name, 0x101, AUDIO_CODEC_MPEG1, NULL);
	...
	/*
	 * 开关音频AD
	 */
	GxPlayer_MediaAdAudioEnable(name, 0x102, AUDIO_CODEC_MPEG1, -1);
	...
	GxPlayer_MediaAdAudioDisable(name);
	...
	/*
	 * 字幕加载
	 */
	PlayerSubPara subpara;
	memset(&subpara, 0, sizeof(PlayerSubPara));
	subpara.type = PLAYER_SUB_TYPE_DVB;
	subpara.pid.pid = 0x103;
	subpara.pid.major = 0x8;
	subpara.pid.minor = 0x19;
	subhandle = GxPlayer_MediaSubLoad(name, &subpara);
	...
	GxPlayer_MediaSubUnLoad(name, subhandle);
	...
	/*
	 * 时间获取
	 */
	PlayTimeInfo tinfo;
	uint64_t duration;
	GxPlayer_MediaGetTime(name, &tinfo);
	GxPlayer_MediaGetDuration(name, &duration);
	/*
	 * 状态获取
	 */
	PlayerStatusInfo sinfo;
	GxPlayer_MediaGetStatus(name, &sinfo);

	/*
	 * 停止播放
	 */
	GxPlayer_MediaStop(name);
}
```

#### 回放录制文件-回放编辑文件-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "playBacker";
	char *srcurl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.pvr";

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR();
	 */
	GxPlayer_ModuleRegisterStreamFILE();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterDemuxerSWTS();

	/*
	 * 启动播放
	 * 编辑文件: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439/01.lst
	 * 编辑文件: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439/event/01.evt
	 * 分段录制: srcurl = /mnt/usb01/GxPvr/TF1_20080918112439/segment/01.seg
	 */
	GxPlayer_MediaPlay(name, srcurl, 0, 50, NULL);

	...

	/*
	 * 停止播放
	 */
	GxPlayer_MediaStop(name);
}
```

#### DVB内存存储(MEM)回放-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "playBacker";
	char  srcurl[64]];
	int dmxid = 0, syncmode = FIXD_APTS_RECOVER;

	/*
	 * 系统配置回放dmxid参数
	 */
	GxBus_ConfigSetInt(PLAYER_CONFIG_PVR_PLAY_DEMUX_ID, dmxid);
	/*
	 * 系统配置回放同步方式
	 */
	GxBus_ConfigSetInt(PLAYER_CONFIG_PVR_PLAYBACK_HWTS_SYNCMODE, syncmode);
	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR();
	 */
	GxPlayer_ModuleRegisterStreamFILE();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterDemuxerSWTS();

	/*
	 * 启动播放
	 * rec_mem: 为MEM录制时的地址
	 * rec_mem: 为MEM录制时的大小
	 */
	snprintf(srcurl, 64, "mem://fmt:4&hwsafe:0&addr:%p&size:%d", rec_mem, rec_size);
	/*
	 * 播放前,获取文件节目信息
	 */
	PlayerProgInfo info;
	memset(&info, 0, sizeof(PlayerProgInfo));
	GxPlayer_MediaGetProgInfoByUrl(srcurl, &info);
	printf("filesize: %lld, duration: %lld\n", info.filesize, info.duration);
	/*
	 * 播放前,获取文件录制信息
	 */
	PlayerRecordConfig config;
	memset(&config, 0, sizeof(PlayerRecordConfig));
	GxPlayer_MediaGetRecordConfigByUrl(srcurl, &config);
	printf("extra pid num: %d, user table len: %d\n", config.ext_info.ext_num, config.ext_data.userdata_len);

	/*
	 * 功能配置回放dmxid参数
	 */
	PlayerPlayPVRConfig config;
	memset(&config, 0, sizeof(PlayerPlayPVRConfig));
	config.demux_id = dmxid;
	GxPlayer_SetPlayPVRConfig(&config);
	/*
	 * 播放文件
	 */
	GxPlayer_MediaPlay(name, srcurl, 0, 50, NULL);
	...

	/*
	 * 播放中,获取文件节目信息
	 */
	memset(&info, 0, sizeof(PlayerProgInfo));
	GxPlayer_MediaGetProgInfoByName(name, &info);
	printf("filesize: %lld, duration: %lld\n", info.filesize, info.duration);
	/*
	 * 播放中,获取文件录制信息
	 */
	memset(&config, 0, sizeof(PlayerRecordConfig));
	GxPlayer_MediaGetRecordConfigByName(name, &config);
	printf("extra pid num: %d, user table len: %d\n", config.ext_info.ext_num, config.ext_data.userdata_len);
	/*
	 * 时间获取
	 */
	PlayTimeInfo tinfo;
	uint64_t duration;
	GxPlayer_MediaGetTime(name, &tinfo);
	GxPlayer_MediaGetDuration(name, &duration);
	/*
	 * 状态获取
	 */
	PlayerStatusInfo sinfo;
	GxPlayer_MediaGetStatus(name, &sinfo);

	/*
	 * 停止播放
	 */
	GxPlayer_MediaStop(name);
}
```

#### FM录制文件回放-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "playBacker";
	char *srcurl = "/mnt/usb01/95000.fm.wav";

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR();
	 */
	GxPlayer_ModuleRegisterStreamFILE();
	GxPlayer_ModuleRegisterDemuxerMP3();

	/*
	 * 播放前,获取文件节目信息
	 */
	PlayerProgInfo info;
	memset(&info, 0, sizeof(PlayerProgInfo));
	GxPlayer_MediaGetProgInfoByUrl(srcurl, &info);
	printf("filesize: %lld, duration: %lld\n", info.filesize, info.duration);
	/*
	 * 启动播放
	 */
	GxPlayer_MediaPlay(name, srcurl, 0, 50, NULL);
	...
	/*
	 * 播放中,获取文件节目信息
	 */
	memset(&info, 0, sizeof(PlayerProgInfo));
	GxPlayer_MediaGetProgInfoByName(name, &info);
	printf("filesize: %lld, duration: %lld\n", info.filesize, info.duration);
	/*
	 * 暂停恢复
	 */
	GxPlayer_MediaPause(name);
	...
	GxPlayer_MediaResume(name);
	...
	/*
	 * 快进快退
	 */
	GxPlayer_MediaSeek(name, 60*1000, SEEK_ORIGIN_SET);
	...
	/*
	 * 时间获取
	 */
	PlayTimeInfo tinfo;
	uint64_t duration;
	GxPlayer_MediaGetTime(name, &tinfo);
	GxPlayer_MediaGetDuration(name, &duration);
	/*
	 * 状态获取
	 */
	PlayerStatusInfo sinfo;
	GxPlayer_MediaGetStatus(name, &sinfo);

	/*
	 * 停止播放
	 */
	GxPlayer_MediaStop(name);
}
```

#### DVB时移-示例代码
```c
#include "gxplayer_module.h"

int GxCore_Startup(int argc, char **argv)
{
	char *name   = "playBacker";
	char *srcurl = "dvbs://fre:0&polar:1&symbol:27500&22k:1&vpid:120&apid:131&pcrpid:120&vcodec:0&acodec:1&tuner:0&scramble:0&pmt:100&tsid:0&dmxid:0&pls_num:0";
	char *dsturl = "/mnt/usb01/GxPvr/TF1_20080918112439.ts.dvr";
	PlayerShiftConfig config0;
	PlayerPlayPVRConfig config1;
	int rec_dmxid = 1, play_dmxid= 0;

	/*
	 * 播放子系统初始
	 */
	GxPlayer_ModuleInit();

	/*
	 * 相关模块注册
	 * 分段录制需要注册: GxPlayer_ModuleRegisterStreamPVR
	 */
	GxPlayer_ModuleRegisterStreamDVB();
	GxPlayer_ModuleRegisterDemuxerHWTS();
	GxPlayer_ModuleRegisterDemuxerSWTS();
	GxPlayer_ModuleRegisterRecorderDVB();
	GxPlayer_ModuleRegisterStreamFILE();

	/*
	 * 时移配置
	 */
	memset(&config0, 0, sizeof(PlayerShiftConfig));
	config0.enable      = 1;
	config0.url         = dsturl;
	config0.shift_dmxid = rec_dmxid;
	GxPlayer_SetTimeShiftConfig(&config0);
	memset(&config1, 0, sizeof(PlayerPlayPVRConfig));
	config1.demux_id = play_dmxid;
	GxPlayer_SetPlayPVRConfig(&config1);

	/*
	 * 启动播放
	 */
	GxPlayer_MediaPlay(name, srcurl, 0, 50, NULL);

	...

	/*
	 * 进入时移
	 */
	GxPlayer_MediaPause(name);
	...
	GxPlayer_MediaResume(name);

	/*
	 * int timems = 60000; //60s
	 * float speed = 2;
	 * 快进快退: GxPlayer_MediaSeek(name, timems, SEEK_ORIGIN_SET);
	 * 倍进倍退: GxPlayer_MediaSpeed(name, speed);
	 */

	/*
	 * 停止播放
	 */
	GxPlayer_MediaStop(name);
}
```
