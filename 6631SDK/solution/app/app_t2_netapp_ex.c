//This .c is generated by python script tool.
//Please do not edit it by yourself.
//sources: netappsmenu-config.json, allconfig.json
//process: app_t2_netapp_ex.c

#include "app.h"
#include "app_main_menu.h"
#include "app_common_2nd_menu.h"

extern status_t app_get_netapps_default_xml(char * name, int32_t *result);

void _internet_entry_init(void)
{
#if NETAPPS_SUPPORT
	int32_t netapp_enable = 0;
#endif
	g_Common2ndEntryCount = 0;
	memset(g_Common2ndEntry, 0, sizeof(g_Common2ndEntry));
#ifdef IPTV_SUPPORT
	if(app_get_netapps_default_xml("gxiptv", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			extern void app_create_gxiptv_menu(void);
			g_Common2ndEntry[g_Common2ndEntryCount].title = "IPTV";
			g_Common2ndEntry[g_Common2ndEntryCount].create = app_create_gxiptv_menu;
			g_Common2ndEntry[g_Common2ndEntryCount].attr = MENU_NONE_ATTR;
			g_Common2ndEntryCount++;
		}
	}
#endif
#ifdef STALKER_SUPPORT
	if(app_get_netapps_default_xml("stalker", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			extern void app_create_stalker_menu(void);
			g_Common2ndEntry[g_Common2ndEntryCount].title = "Stalker";
			g_Common2ndEntry[g_Common2ndEntryCount].create = app_create_stalker_menu;
			g_Common2ndEntry[g_Common2ndEntryCount].attr = MENU_NETWORK_CHECK;
			g_Common2ndEntryCount++;
		}
	}
#endif
#ifdef SUBTTRANS_SUPPORT
	if(app_get_netapps_default_xml("subttrans", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			extern void app_subttrans_setting_menu_exec(void);
			g_Common2ndEntry[g_Common2ndEntryCount].title = "Subtitle Translation";
			g_Common2ndEntry[g_Common2ndEntryCount].create = app_subttrans_setting_menu_exec;
			g_Common2ndEntry[g_Common2ndEntryCount].attr = MENU_NETWORK_CHECK;
			g_Common2ndEntryCount++;
		}
	}
#endif
#ifdef XTREAM_SUPPORT
	if(app_get_netapps_default_xml("xtream", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			extern void app_create_xtream_menu(void);
			g_Common2ndEntry[g_Common2ndEntryCount].title = "Xtream";
			g_Common2ndEntry[g_Common2ndEntryCount].create = app_create_xtream_menu;
			g_Common2ndEntry[g_Common2ndEntryCount].attr = MENU_NETWORK_CHECK;
			g_Common2ndEntryCount++;
		}
	}
#endif
#ifdef RSS_SUPPORT
	if(app_get_netapps_default_xml("rss", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			extern void app_rss_create_dialog(void);
			g_Common2ndEntry[g_Common2ndEntryCount].title = "RSS";
			g_Common2ndEntry[g_Common2ndEntryCount].create = app_rss_create_dialog;
			g_Common2ndEntry[g_Common2ndEntryCount].attr = MENU_NETWORK_CHECK;
			g_Common2ndEntryCount++;
		}
	}
#endif
#ifdef FREEIPTV_SUPPORT
	if(app_get_netapps_default_xml("freeiptv", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			extern void app_create_freeiptv_menu(void);
			g_Common2ndEntry[g_Common2ndEntryCount].title = "Free IPTV";
			g_Common2ndEntry[g_Common2ndEntryCount].create = app_create_freeiptv_menu;
			g_Common2ndEntry[g_Common2ndEntryCount].attr = MENU_NONE_ATTR;
			g_Common2ndEntryCount++;
		}
	}
#endif
	return;
}

int _internet_entry_count_get(void)
{
	static int ret = -1;
#if NETAPPS_SUPPORT
	int netapp_enable = 0;

#endif
	if(ret >= 0)
		return ret;
	ret = 0;
#ifdef IPTV_SUPPORT
	if(app_get_netapps_default_xml("gxiptv", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			ret++;
		}
	}
#endif
#ifdef STALKER_SUPPORT
	if(app_get_netapps_default_xml("stalker", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			ret++;
		}
	}
#endif
#ifdef SUBTTRANS_SUPPORT
	if(app_get_netapps_default_xml("subttrans", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			ret++;
		}
	}
#endif
#ifdef XTREAM_SUPPORT
	if(app_get_netapps_default_xml("xtream", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			ret++;
		}
	}
#endif
#ifdef RSS_SUPPORT
	if(app_get_netapps_default_xml("rss", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			ret++;
		}
	}
#endif
#ifdef FREEIPTV_SUPPORT
	if(app_get_netapps_default_xml("freeiptv", &netapp_enable) == GXCORE_SUCCESS)
	{
		if(netapp_enable == 1)
		{
			ret++;
		}
	}
#endif
	return ret;
}
